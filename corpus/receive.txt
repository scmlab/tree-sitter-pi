==================
Simple
==================

name = c?x . end

---

(program
    (proc_declaration (process_name)
        (recv (name) (name)
            (end))))

==================
Parenthesized
==================

name = (c?x . end)

---

(program
    (proc_declaration (process_name)
        (recv (name) (name)
            (end))))

==================
Call
==================

name = c?x . name

---

(program
    (proc_declaration (process_name)
        (recv (name) (name)
            (call (process_name)))))

==================
New channel - 1
==================

name = (nu x) c?x . end

---

(program
    (proc_declaration (process_name)
        (nu (name)
            (recv (name) (name)
                (end)))))

==================
New channel - 2
==================

name =  c?x . (nu x) end

---

(program
    (proc_declaration (process_name)
        (recv (name) (name)
            (nu (name)
                (end)))))

==================
Parellel - 1
==================

name =  (nu x) end | end

---

(program
    (proc_declaration (process_name)
        (par
            (nu (name)
                (end))
            (end))))

==================
Parellel - 2
==================

name =  (nu x) (end | end)

---

(program
    (proc_declaration (process_name)
        (nu (name)
            (par
                (end)
                (end)))))
